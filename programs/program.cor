import strings as s

rom string[] = "Hello, world!"
pre string = $string[0]

ldr g, s.string0
lpt a, rom

printloop:
  cmp a, 0
  joc equal, stop
  str a, UART
  lsr a, 8, b
  str b, UART
  add g, 1
  lpt a, rom
  jmp printloop

stop:
  jmp stop
